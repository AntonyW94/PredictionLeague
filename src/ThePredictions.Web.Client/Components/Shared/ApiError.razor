@if (!string.IsNullOrEmpty(Message))
{
    <div class="message-box-solid error whitespace-pre-wrap" data-test-id="api-error" role="alert">
        @Message
    </div>
}

@code {
    [Parameter]
    public string? Message { get; set; }
}