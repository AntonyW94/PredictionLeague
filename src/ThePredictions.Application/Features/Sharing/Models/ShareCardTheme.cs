namespace ThePredictions.Application.Features.Sharing.Models;

/// <summary>
/// Which colour scheme a share card is rendered in - mirrors the player's selected UI theme so
/// the shared image matches the app they are looking at when they tap Share.
/// </summary>
public enum ShareCardTheme
{
    Light,
    Dark
}
